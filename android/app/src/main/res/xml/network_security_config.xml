<?xml version="1.0" encoding="utf-8"?>
<!--
  AdaptiveFit Android network security policy.

  Default: all traffic must be HTTPS (production talks to Railway, which is
  always HTTPS).

  Exception: cleartext HTTP is allowed ONLY for the dev LAN host, so
  `pnpm cap:sync:dev` builds can connect to the Next.js dev server running
  on your laptop. Other LAN hosts and the wider internet still require HTTPS.

  To change the dev IP later, edit it here and re-run:
      pnpm cap:sync:dev

  Reference:
    https://developer.android.com/training/articles/security-config
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- LAN dev server (Next.js on your laptop). HTTP-only is fine on LAN. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.0.20</domain>
        <!-- Localhost fallback for emulator (10.0.2.2 = host loopback on emulator). -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
